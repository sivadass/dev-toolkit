# File Drop Control Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a shared drag-and-drop file control that transforms into file chip(s), document single + multi in the UI kit, and use the single variant on the image compressor page.

**Architecture:** Extend the existing static `design/` prototype. Styles live in `components.css` as `.file-drop*`. Behavior lives in `assets/scripts/file-drop.js`, which initializes every `[data-file-drop]` root, keeps an internal `File[]`, renders chips (image thumbnail or icon fallback), and restores the idle dropzone when empty. No bundler; pages load the script with a plain `<script>` tag.

**Tech Stack:** HTML5, CSS custom properties (existing tokens), vanilla JS, Material Symbols Rounded (already loaded on pages).

## Global Constraints

- Filenames: **kebab-case** only
- Brand colors only via tokens from `design/assets/styles/tokens.css` — no hard-coded brand hex in components
- Spec source of truth: `docs/superpowers/specs/2026-08-04-file-drop-design.md`
- Minimal JS: drag highlight, pick, chip render, remove — no custom MIME validation beyond `accept`, no compression logic
- Product UI name: **DevToolkit**
- Light theme only

---

## File map

| File | Responsibility |
|------|----------------|
| `design/assets/styles/components.css` | `.file-drop*` visual styles (idle, dragging, chips) |
| `design/assets/scripts/file-drop.js` | Initialize dropzones, drag state, file list, chip DOM |
| `design/ui-kit.html` | Live single + multi demos in a new File drop section |
| `design/image-compressor.html` | Replace native file input with single image dropzone; load script |

---

### Task 1: File drop styles

**Files:**
- Modify: `design/assets/styles/components.css` (append after existing `.state-grid__section h2` block)

**Interfaces:**
- Consumes: tokens `--color-*`, `--space-*`, `--radius-*`, `--motion-fast`
- Produces: CSS classes `.file-drop`, `.file-drop.is-dragging`, `.file-drop__input`, `.file-drop__idle`, `.file-drop__icon`, `.file-drop__title`, `.file-drop__subtitle`, `.file-drop__list`, `.file-drop__chip`, `.file-drop__thumb`, `.file-drop__meta`, `.file-drop__name`, `.file-drop__size`, `.file-drop__remove` (names must not change in later tasks)

- [ ] **Step 1: Append file-drop styles to `components.css`**

Append exactly this block at the end of `design/assets/styles/components.css`:

```css
.file-drop {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  min-height: 160px;
  padding: var(--space-5);
  border: 1.5px dashed var(--color-border);
  border-radius: var(--radius-panel);
  background: var(--color-surface-muted);
  transition:
    background var(--motion-fast),
    border-color var(--motion-fast),
    box-shadow var(--motion-fast);
}

.file-drop:hover,
.file-drop:focus-within {
  background: var(--color-primary-soft);
  border-color: rgba(12, 10, 93, 0.2);
}

.file-drop.is-dragging {
  background: var(--color-accent-soft);
  border-color: var(--color-accent);
  box-shadow: inset 0 0 0 1px var(--color-accent);
}

.file-drop__input {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  cursor: pointer;
  z-index: 1;
}

.file-drop.has-files .file-drop__input {
  pointer-events: none;
}

.file-drop__idle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  text-align: center;
  pointer-events: none;
  min-height: 120px;
}

.file-drop__icon {
  display: grid;
  place-items: center;
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 12px;
  background: rgba(12, 10, 93, 0.06);
  color: var(--color-primary);
  font-size: 1.4rem;
  line-height: 1;
}

.file-drop__title {
  margin: 0;
  font-weight: 600;
  color: var(--color-primary);
}

.file-drop__subtitle {
  margin: 0;
  font-size: 0.875rem;
  color: var(--color-text-muted);
}

.file-drop__subtitle span {
  color: var(--color-primary);
  text-decoration: underline;
  text-underline-offset: 2px;
}

.file-drop__list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
  position: relative;
  z-index: 2;
}

.file-drop__chip {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-3);
  border-radius: var(--radius-control);
  background: var(--color-surface);
  border: 1px solid var(--color-border);
}

.file-drop__thumb {
  flex-shrink: 0;
  display: grid;
  place-items: center;
  width: 40px;
  height: 40px;
  border-radius: 8px;
  overflow: hidden;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  font-size: 1.25rem;
  line-height: 1;
}

.file-drop__thumb img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.file-drop__meta {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.file-drop__name {
  margin: 0;
  font-weight: 600;
  color: var(--color-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.file-drop__size {
  margin: 0;
  font-size: 0.8125rem;
  color: var(--color-text-muted);
}

.file-drop__remove {
  flex-shrink: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  padding: 0;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  background: transparent;
  color: var(--color-primary);
  cursor: pointer;
  position: relative;
  z-index: 3;
}

.file-drop__remove:hover {
  background: var(--color-primary-soft);
}
```

Note: `rgba(12, 10, 93, 0.06)` and `rgba(12, 10, 93, 0.2)` match existing patterns already used in `components.css` for `.tool-card__icon` / hover — acceptable as primary-derived soft values already present in the file. Prefer tokens where a token exists (`--color-primary-soft`, `--color-accent`, etc.).

- [ ] **Step 2: Visual smoke-check (optional until HTML exists)**

Confirm the file ends with the new block and contains no syntax errors (balanced braces).

- [ ] **Step 3: Commit**

```bash
git add design/assets/styles/components.css
git commit -m "$(cat <<'EOF'
style(design): add file-drop dropzone and chip styles

EOF
)"
```

---

### Task 2: File drop script

**Files:**
- Create: `design/assets/scripts/file-drop.js`

**Interfaces:**
- Consumes: DOM roots matching `[data-file-drop]`; optional `data-multiple="true"` or `multiple` on the input; child hooks `[data-file-drop-idle]`, `[data-file-drop-list]`, `.file-drop__input`
- Produces: `initFileDrops()` called on `DOMContentLoaded`; each root gains class `has-files` when files present; chips with structure below

Chip DOM (produced by script):

```html
<li class="file-drop__chip">
  <div class="file-drop__thumb">…img or material icon…</div>
  <div class="file-drop__meta">
    <p class="file-drop__name">filename.ext</p>
    <p class="file-drop__size">12.3 KB</p>
  </div>
  <button type="button" class="file-drop__remove" aria-label="Remove filename.ext">
    <span class="material-symbols-rounded" aria-hidden="true">close</span>
  </button>
</li>
```

- [ ] **Step 1: Create `design/assets/scripts/file-drop.js`**

```js
(function () {
  function formatSize(bytes) {
    if (bytes < 1024) return bytes + " B";
    if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + " KB";
    return (bytes / (1024 * 1024)).toFixed(1) + " MB";
  }

  function isMultiple(root, input) {
    return root.getAttribute("data-multiple") === "true" || input.multiple;
  }

  function revokeThumbs(list) {
    list.querySelectorAll("img[data-object-url]").forEach(function (img) {
      URL.revokeObjectURL(img.getAttribute("data-object-url"));
    });
  }

  function render(root, idle, list, files) {
    revokeThumbs(list);
    list.innerHTML = "";

    if (!files.length) {
      root.classList.remove("has-files");
      idle.hidden = false;
      list.hidden = true;
      return;
    }

    root.classList.add("has-files");
    idle.hidden = true;
    list.hidden = false;

    files.forEach(function (file, index) {
      var li = document.createElement("li");
      li.className = "file-drop__chip";

      var thumb = document.createElement("div");
      thumb.className = "file-drop__thumb";

      if (file.type.indexOf("image/") === 0) {
        var url = URL.createObjectURL(file);
        var img = document.createElement("img");
        img.src = url;
        img.alt = "";
        img.setAttribute("data-object-url", url);
        thumb.appendChild(img);
      } else {
        var icon = document.createElement("span");
        icon.className = "material-symbols-rounded";
        icon.setAttribute("aria-hidden", "true");
        icon.textContent = "draft";
        thumb.appendChild(icon);
      }

      var meta = document.createElement("div");
      meta.className = "file-drop__meta";

      var name = document.createElement("p");
      name.className = "file-drop__name";
      name.textContent = file.name;

      var size = document.createElement("p");
      size.className = "file-drop__size";
      size.textContent = formatSize(file.size);

      meta.appendChild(name);
      meta.appendChild(size);

      var remove = document.createElement("button");
      remove.type = "button";
      remove.className = "file-drop__remove";
      remove.setAttribute("aria-label", "Remove " + file.name);
      remove.innerHTML =
        '<span class="material-symbols-rounded" aria-hidden="true">close</span>';
      remove.addEventListener("click", function () {
        files.splice(index, 1);
        syncInput(root, files);
        render(root, idle, list, files);
      });

      li.appendChild(thumb);
      li.appendChild(meta);
      li.appendChild(remove);
      list.appendChild(li);
    });
  }

  function syncInput(root, files) {
    var input = root.querySelector(".file-drop__input");
    if (!input) return;
    try {
      var dt = new DataTransfer();
      files.forEach(function (f) {
        dt.items.add(f);
      });
      input.files = dt.files;
    } catch (err) {
      /* DataTransfer assignment unsupported — UI still works */
    }
  }

  function addFiles(root, idle, list, files, incoming) {
    var input = root.querySelector(".file-drop__input");
    var multiple = isMultiple(root, input);
    var next = multiple ? files.concat(Array.prototype.slice.call(incoming)) : [incoming[0]].filter(Boolean);
    files.length = 0;
    next.forEach(function (f) {
      files.push(f);
    });
    syncInput(root, files);
    render(root, idle, list, files);
  }

  function initRoot(root) {
    var input = root.querySelector(".file-drop__input");
    var idle = root.querySelector("[data-file-drop-idle]");
    var list = root.querySelector("[data-file-drop-list]");
    if (!input || !idle || !list) return;

    if (root.getAttribute("data-multiple") === "true") {
      input.multiple = true;
    }

    var files = [];
    var dragDepth = 0;

    ["dragenter", "dragover", "dragleave", "drop"].forEach(function (type) {
      root.addEventListener(type, function (event) {
        event.preventDefault();
        event.stopPropagation();
      });
    });

    root.addEventListener("dragenter", function () {
      dragDepth += 1;
      root.classList.add("is-dragging");
    });

    root.addEventListener("dragleave", function () {
      dragDepth -= 1;
      if (dragDepth <= 0) {
        dragDepth = 0;
        root.classList.remove("is-dragging");
      }
    });

    root.addEventListener("drop", function (event) {
      dragDepth = 0;
      root.classList.remove("is-dragging");
      var dropped = event.dataTransfer && event.dataTransfer.files;
      if (dropped && dropped.length) {
        addFiles(root, idle, list, files, dropped);
      }
    });

    input.addEventListener("change", function () {
      if (input.files && input.files.length) {
        addFiles(root, idle, list, files, input.files);
      }
    });
  }

  function initFileDrops() {
    document.querySelectorAll("[data-file-drop]").forEach(initRoot);
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initFileDrops);
  } else {
    initFileDrops();
  }
})();
```

- [ ] **Step 2: Syntax check**

Run: `node --check design/assets/scripts/file-drop.js`  
Expected: no output, exit code 0

- [ ] **Step 3: Commit**

```bash
git add design/assets/scripts/file-drop.js
git commit -m "$(cat <<'EOF'
feat(design): add minimal file-drop drag-and-drop script

EOF
)"
```

---

### Task 3: UI kit demos (single + multi)

**Files:**
- Modify: `design/ui-kit.html`

**Interfaces:**
- Consumes: `.file-drop*` CSS from Task 1; `file-drop.js` from Task 2
- Produces: section `#file-drop-heading` with two live fields — `#demo-file-single` (single, image accept) and `#demo-file-multi` (multi, any file)

- [ ] **Step 1: Add File drop section and script to `ui-kit.html`**

Insert a new `<section>` after the Fields section (before Alerts), and add the script before `</body>`:

In the Fields section area — after the Fields `</section>` and before Alerts — insert:

```html
          <section class="state-grid__section" aria-labelledby="file-drop-heading">
            <h2 id="file-drop-heading">File drop</h2>
            <div class="field">
              <label class="field__label" for="demo-file-single">Single image</label>
              <div class="file-drop" data-file-drop data-multiple="false">
                <input
                  class="file-drop__input"
                  id="demo-file-single"
                  type="file"
                  accept="image/png,image/jpeg,image/webp,image/gif"
                />
                <div class="file-drop__idle" data-file-drop-idle>
                  <span class="file-drop__icon material-symbols-rounded" aria-hidden="true">upload_file</span>
                  <p class="file-drop__title">Drop an image here</p>
                  <p class="file-drop__subtitle">or <span>browse</span></p>
                </div>
                <ul class="file-drop__list" data-file-drop-list hidden></ul>
              </div>
              <p class="field__hint">Single file — new pick replaces the previous. Thumbnail when image.</p>
            </div>
            <div class="field">
              <label class="field__label" for="demo-file-multi">Multiple files</label>
              <div class="file-drop" data-file-drop data-multiple="true">
                <input class="file-drop__input" id="demo-file-multi" type="file" />
                <div class="file-drop__idle" data-file-drop-idle>
                  <span class="file-drop__icon material-symbols-rounded" aria-hidden="true">upload_file</span>
                  <p class="file-drop__title">Drop files here</p>
                  <p class="file-drop__subtitle">or <span>browse</span></p>
                </div>
                <ul class="file-drop__list" data-file-drop-list hidden></ul>
              </div>
              <p class="field__hint">Multiple files — chips stack; non-images use a draft icon.</p>
            </div>
          </section>
```

Before `</body>`, add:

```html
    <script src="assets/scripts/file-drop.js"></script>
```

- [ ] **Step 2: Manual verify**

Run: `python3 -m http.server 4173 --directory design`  
Open: `http://localhost:4173/ui-kit.html`

Checklist:
- [ ] Single: browse/drop one image → chip with thumbnail; replace works; remove restores idle
- [ ] Multi: add several files → multiple chips; non-image shows draft icon; remove one leaves the rest
- [ ] Dragging over zone shows `is-dragging` accent treatment
- [ ] Keyboard: Tab focuses file input; focus ring visible on dropzone via `:focus-within`

- [ ] **Step 3: Commit**

```bash
git add design/ui-kit.html
git commit -m "$(cat <<'EOF'
feat(design): document single and multi file-drop in UI kit

EOF
)"
```

---

### Task 4: Wire image compressor

**Files:**
- Modify: `design/image-compressor.html`

**Interfaces:**
- Consumes: same markup contract as UI kit single demo; `file-drop.js`
- Produces: `#image-file` single dropzone with `accept="image/png,image/jpeg"`

- [ ] **Step 1: Replace native file field and load script**

Replace this block:

```html
            <div class="field">
              <label class="field__label" for="image-file">Image file</label>
              <input class="field__control" id="image-file" type="file" accept="image/png,image/jpeg" />
              <p class="field__hint">PNG or JPG up to a few MB (prototype — no processing yet).</p>
            </div>
```

With:

```html
            <div class="field">
              <label class="field__label" for="image-file">Image file</label>
              <div class="file-drop" data-file-drop data-multiple="false">
                <input
                  class="file-drop__input"
                  id="image-file"
                  type="file"
                  accept="image/png,image/jpeg"
                />
                <div class="file-drop__idle" data-file-drop-idle>
                  <span class="file-drop__icon material-symbols-rounded" aria-hidden="true">upload_file</span>
                  <p class="file-drop__title">Drop an image here</p>
                  <p class="file-drop__subtitle">or <span>browse</span></p>
                </div>
                <ul class="file-drop__list" data-file-drop-list hidden></ul>
              </div>
              <p class="field__hint">PNG or JPG up to a few MB (prototype — no processing yet).</p>
            </div>
```

Change the scripts at the bottom from:

```html
    <script src="assets/scripts/nav.js"></script>
```

To:

```html
    <script src="assets/scripts/nav.js"></script>
    <script src="assets/scripts/file-drop.js"></script>
```

- [ ] **Step 2: Manual verify**

Open: `http://localhost:4173/image-compressor.html`

Checklist:
- [ ] Native file control is gone; dropzone matches UI kit single styling
- [ ] Drop/browse PNG or JPG → thumbnail chip with name/size; remove restores idle
- [ ] Mobile menu (`nav.js`) still opens/closes
- [ ] Original/Compressed panels remain placeholders (unchanged)

- [ ] **Step 3: Commit**

```bash
git add design/image-compressor.html
git commit -m "$(cat <<'EOF'
feat(design): use file-drop control on image compressor

EOF
)"
```

---

## Spec coverage (self-review)

| Spec requirement | Task |
| --- | --- |
| Shared `.file-drop` markup | 3, 4 |
| Styles in `components.css` | 1 |
| Script `file-drop.js` | 2 |
| UI kit single + multi | 3 |
| Compressor single + PNG/JPG accept | 4 |
| Idle → chip transform, drag highlight, remove | 2 |
| Image thumbnail / icon fallback | 2 |
| Internal array for multi remove | 2 |
| No custom MIME validation / no compression | 2, 4 (explicit non-goals) |
| a11y label, focus-within, remove `aria-label` | 2, 3, 4 |
| Tokens / no new brand hex outside tokens | 1 (uses tokens; primary soft rgba matches existing file pattern) |

No placeholders remain. Class and `data-*` names are consistent across tasks.
